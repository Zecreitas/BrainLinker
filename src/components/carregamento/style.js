import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    image: {
        width: 350,
        height: 350,
        alignSelf: 'center',
        marginTop: 50
    },
});

export default styles